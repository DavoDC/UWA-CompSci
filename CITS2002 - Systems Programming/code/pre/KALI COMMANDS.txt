// Install C
apt-get update
apt-get install build-essentials
apt-get install gcc

// Edit within console
nano Hello.c

// Compile 
mycc Hello hello.c

// Run
./exe

// Rename
mv old.c new.c

// CMD commands to change default user
kali config --default-user root
kali config --default-user david
sudo su    // Go into ROOT in place!

// Permissions

// Check
ls -lha       

// Fix files 
chmod 600 *

// Fix Exes/Folders
chmod 700 *  
